---
import BaseHead from "@/components/BaseHead.astro";
import Footer from "@/components/Footer.astro";
import Header from "@/components/Header.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import { getCollection } from "astro:content";

// 获取最新3篇文章
const latestPosts = (await getCollection("blog"))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 3);
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<link rel="stylesheet" href="/fontawesome/fontawesome.min.css" />
		<link rel="stylesheet" href="/fontawesome/brands.min.css" />
		<link rel="stylesheet" href="/fontawesome/solid.min.css" />
	</head>
	<body>
		<div class="page-root">
			<div class="hero">
				<div class="hero-overlay">
					<Header />
					<div class="hero-content">
						<h1 class="title">{SITE_TITLE}</h1>
						<p class="subtitle" id="hitokoto">加载一言中…</p>
					</div>
				</div>
				<!-- 将联系组件移到hero容器内部 -->
				<div class="contact-container">
					<a
						href="https://github.com/your-username"
						class="contact-icon"
						title="GitHub"
					>
						<i class="fab fa-github"></i>
					</a>
					<a
						href="mailto:contact@example.com"
						class="contact-icon"
						title="Email"
					>
						<i class="fas fa-at"></i>
					</a>
					<div class="contact-divider">|</div>
					<div class="contact-icon-with-tooltip" title="图片">
						<a href="#" class="contact-icon">
							<i class="fas fa-image"></i>
						</a>
						<!-- 气泡聊天框子组件 -->
						<div class="tooltip-bubble">
							<div class="tooltip-content"></div>
							<div class="tooltip-arrow"></div>
						</div>
					</div>
				</div>
			</div>

			<div class="latest-posts-section">
				<div class="section-header">
					<div class="section-header-main">
						<h2 class="section-title">最新文章</h2>
						<p class="section-subtitle">最近更新的内容，一眼就能找到想看的。</p>
					</div>
					<a href="/blog/" class="view-all-button">
						<span>查看更多</span>
						<span class="view-all-button-icon">→</span>
					</a>
				</div>

				<div class="posts-grid">
					{
						latestPosts.map((post) => (
							<a href={`/blog/${post.id}/`} class="post-card">
								{post.data.heroImage && (
									<div class="post-image-wrapper">
										<img
											src={post.data.heroImage.toString()}
											alt={post.data.title}
											class="post-image"
											loading="lazy"
										/>
									</div>
								)}
								<div class="post-content">
									<!-- 分类标签 -->
									{post.data.categories && post.data.categories.length > 0 && (
										<span class="post-category">
											{post.data.categories[0]}
										</span>
									)}
									
									<h3 class="post-title">
										{post.data.title}
									</h3>
									
									<!-- 标签展示 -->
									{post.data.tags && post.data.tags.length > 0 && (
										<div class="post-tags">
											{post.data.tags.map((tag, index) => (
												<span class="post-tag">
													{tag}
												</span>
											))}
										</div>
									)}
									
									<div class="post-meta">
										<span class="post-date">
											<svg
												viewBox="0 0 24 24"
												fill="none"
												stroke="currentColor"
												stroke-width="2"
												width="14"
												height="14"
											>
												<rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
												<line x1="16" y1="2" x2="16" y2="6"/>
												<line x1="8" y1="2" x2="8" y2="6"/>
												<line x1="3" y1="10" x2="21" y2="10"/>
											</svg>
											<span>{new Date(post.data.pubDate).toLocaleDateString('zh-CN')}</span>
										</span>
									</div>
								</div>
							</a>
						))
					}
				</div>
			</div>

			<Footer />
		</div>
		<script>
			// 在客户端执行
			let imageCopyright = "";
			let imageTitle = "";

			async function loadCopyrightInfo() {
				try {
					const response = await fetch(
						"https://bing.api.iswxl.cn/copyright.php",
					);
					const data = await response.json();
					imageCopyright = data.copyright || "";
					imageTitle = data.title || "";

					// 更新DOM中的内容
					const tooltipContent =
						document.querySelector(".tooltip-content");
					if (tooltipContent) {
						tooltipContent.innerHTML = `<p style="font-size: 1.2rem">${imageTitle}</p><p>${imageCopyright}</p>`;
					}
				} catch (error) {
					console.error("获取版权信息失败:", error);
				}
			}

			// 页面加载完成后执行
			if (typeof window !== "undefined") {
				loadCopyrightInfo();
			}

			if (typeof window !== "undefined") {
				const hero = document.querySelector(".hero");

				if (hero) {
					hero.setAttribute(
						"style",
						"background-image: url('https://bing.api.iswxl.cn');",
					);
				}

				// 增强版打字机效果函数 - 支持删除和循环
				function advancedTypeWriter(
					element: HTMLElement,
					texts: string[],
					typeSpeed: number = 150,
					deleteSpeed: number = 100,
					pauseTime: number = 2000,
				) {
					let textIndex = 0;
					let charIndex = 0;
					let isDeleting = false;
					let currentText = texts[0];

					function type() {
						// 更新当前显示的文本
						const fullText = isDeleting
							? currentText.substring(0, charIndex - 1)
							: currentText.substring(0, charIndex + 1);

						element.textContent = fullText;
						element.setAttribute("data-text", fullText);

						// 添加视觉反馈类
						if (isDeleting) {
							element.classList.add("changing");
						} else {
							element.classList.remove("changing");
						}

						if (!isDeleting) {
							charIndex++;
							// 打字完成
							if (charIndex === currentText.length) {
								isDeleting = true;
								setTimeout(type, pauseTime); // 暂停后开始删除
								return;
							}
						} else {
							charIndex--;
							// 删除完成
							if (charIndex === 0) {
								isDeleting = false;
								textIndex = (textIndex + 1) % texts.length; // 循环到下一个文本
								currentText = texts[textIndex];
								element.classList.remove("changing");
								setTimeout(type, 500); // 删除完成后短暂暂停
								return;
							}
						}

						// 设置下次调用的时间间隔
						const timeout = isDeleting ? deleteSpeed : typeSpeed;
						setTimeout(type, timeout);
					}

					// 启动打字机
					type();
				}

				// 获取多条一言并应用循环打字机效果
				async function startTypewriterLoop() {
					try {
						// 获取多条一言
						const promises = Array.from({ length: 5 }, () =>
							fetch("https://v1.hitokoto.cn?encode=text").then(
								(res) => res.text(),
							),
						);

						const texts = await Promise.all(promises);
						const el = document.getElementById("hitokoto");

						if (el) {
							// 应用循环打字机效果
							advancedTypeWriter(el, texts, 120, 80, 2500);
						}
					} catch (error) {
						const el = document.getElementById("hitokoto");
						if (el) {
							el.textContent = "一言加载失败";
						}
					}
				}

				// 启动循环打字机
				startTypewriterLoop();
			}
		</script>
		<style>
			/* 首页特有样式 - 移除冗余代码，使用全局样式和CSS变量 */
			
			/* 页面布局 */
			.page-root {
				min-height: 100vh;
				display: flex;
				flex-direction: column;
			}
			
			/* 英雄区域特有样式 */
			.hero {
				position: relative;
				min-height: 100vh;
				background-size: cover;
				background-position: center center;
				background-repeat: no-repeat;
			}
			
			.hero-overlay {
				position: relative;
				min-height: inherit;
				display: flex;
				flex-direction: column;
			}
			
			.hero-content {
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				padding: 0;
				color: #fff;
				flex: 1;
			}
			
			.hero-content .title {
				margin: 0 0 1rem;
				font-size: 2.8rem;
				text-shadow: 0 8px 24px rgba(0, 0, 0, 0.6);
			}
			
			.hero-content .subtitle {
				margin: 0;
				font-size: 1.2rem;
				max-width: 640px;
				text-shadow: 0 4px 16px rgba(0, 0, 0, 0.6);
				position: relative;
				min-height: 1.5em;
				transition: all 0.1s ease;
			}

			/* 打字机光标效果 */
			.hero-content .subtitle::after {
				content: "|";
				animation: blink 0.8s infinite;
				color: #fff;
				margin-left: 4px;
				text-shadow: 0 0 10px rgba(255, 255, 255, 0.8);
				opacity: 1;
			}

			.hero-content .subtitle[data-deleting="true"]::after {
				content: "⌫";
				color: #ff6b6b;
				animation: blinkDelete 0.6s infinite;
			}

			@keyframes blink {
				0%, 100% { opacity: 1; }
				50% { opacity: 0; }
			}

			@keyframes blinkDelete {
				0%, 100% { 
					opacity: 1; 
					transform: scale(1.2);
				}
				50% { 
					opacity: 0.3; 
					transform: scale(1);
				}
			}

			.hero-content .subtitle.changing {
				opacity: 0.8;
				transform: translateY(-2px);
			}

			/* 联系组件样式 */
			.contact-container {
				position: absolute;
				bottom: 2rem;
				right: 22rem;
				display: flex;
				align-items: center;
				background: rgba(255, 255, 255, 0.55);
				border-radius: 26px;
				padding: 0.5rem 0.8rem;
				box-shadow: 0 4px 20px rgba(0, 0, 0, 0.18);
				backdrop-filter: blur(12px);
				-webkit-backdrop-filter: blur(12px);
				z-index: 10;
				gap: 0.4rem;
				border: 1px solid rgba(255, 255, 255, 0.3);
			}

			.contact-icon {
				width: 32px;
				height: 32px;
				display: flex;
				align-items: center;
				justify-content: center;
				color: rgb(80, 80, 80);
				text-decoration: none;
				font-size: 1rem;
				transition: all 0.2s ease;
				border-radius: 8px;
			}

			.contact-icon:hover {
				background: rgba(255, 255, 255, 0.5);
				transform: translateY(-1px);
				color: #333;
			}

			.contact-divider {
				color: rgba(80, 80, 80, 0.6);
				font-size: 1.2rem;
				font-weight: bold;
				line-height: 1;
				user-select: none;
				margin: 0 0.3rem;
			}

			/* 气泡聊天框样式 */
			.contact-icon-with-tooltip {
				position: relative;
				display: inline-block;
			}

			.tooltip-bubble {
				position: absolute;
				bottom: 120%;
				right: 50%;
				transform: translateX(50%);
				background: rgba(255, 255, 255, 0.95);
				border-radius: 12px;
				padding: 0.8rem 1.2rem;
				box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
				backdrop-filter: blur(10px);
				-webkit-backdrop-filter: blur(10px);
				border: 1px solid rgba(255, 255, 255, 0.3);
				opacity: 0;
				visibility: hidden;
				transition: all 0.3s cubic-bezier(0.17, 0.67, 0.88, 1.06);
				z-index: 20;
				min-width: 200px;
				max-width: 280px;
				margin-bottom: 12px;
			}

			.tooltip-bubble .tooltip-content {
				color: rgb(80, 80, 80);
				font-size: 0.9rem;
				line-height: 1.5;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				height: 100%;
			}

			.tooltip-bubble .tooltip-content p {
				margin: 0.3rem 0;
			}

			.tooltip-arrow {
				position: absolute;
				top: 100%;
				right: 50%;
				transform: translateX(50%) rotate(45deg);
				width: 10px;
				height: 10px;
				background: rgba(255, 255, 255, 0.95);
				border-right: 1px solid rgba(255, 255, 255, 0.3);
				border-bottom: 1px solid rgba(255, 255, 255, 0.3);
				z-index: 1;
			}

			.contact-icon-with-tooltip:hover .tooltip-bubble {
				opacity: 1;
				visibility: visible;
				transform: translateX(50%) translateY(-8px);
			}

			.tooltip-bubble {
				transform: translateX(50%) translateY(10px);
			}

			.contact-icon-with-tooltip:hover .tooltip-bubble {
				transform: translateX(50%) translateY(-8px);
			}

			/* 隐藏不需要的旧样式 */
			.contact-buttons, .contact-btn, .contact-separator {
				display: none;
			}

			/* 最新文章区域 - 使用全局样式 */
			.latest-posts-section {
				padding: 3.5rem 1.5rem 3rem;
				max-width: 1120px;
				margin: 0 auto 3rem;
			}

			.section-header {
				display: flex;
				justify-content: space-between;
				align-items: center;
				gap: 1.5rem;
				margin-bottom: 2.5rem;
			}

			.section-header-main {
				display: flex;
				flex-direction: column;
				gap: 0.45rem;
			}

			.section-title {
				font-size: 1.8rem;
				font-weight: 700;
				color: rgb(var(--black));
				margin: 0;
				position: relative;
			}

			.section-title::after {
				content: "";
				position: absolute;
				bottom: -0.4rem;
				left: 0;
				width: 50px;
				height: 2px;
				background: linear-gradient(90deg, var(--accent), rgba(var(--accent), 0.1));
				border-radius: 2px;
			}

			.section-subtitle {
				margin: 0;
				font-size: 0.95rem;
				color: rgb(var(--gray));
				max-width: 420px;
			}

			.view-all-button {
				display: inline-flex;
				align-items: center;
				justify-content: center;
				gap: 0.4rem;
				padding: 0.6rem 1.3rem;
				border-radius: 999px;
				font-size: 0.9rem;
				font-weight: 600;
				color: var(--accent);
				background: rgba(var(--accent), 0.06);
				border: 1px solid rgba(var(--accent), 0.25);
				text-decoration: none;
				transition: var(--transition-base);
				white-space: nowrap;
			}

			.view-all-button:hover {
				background: var(--accent);
				color: #fff;
				transform: translateY(-1px);
				box-shadow: 0 10px 30px rgba(74, 144, 226, 0.35);
			}

			.view-all-button-icon {
				font-size: 1rem;
				line-height: 1;
			}

			/* 文章网格 - 使用全局卡片样式 */
			.posts-grid {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
				gap: 2rem;
				max-width: 1200px;
				margin: 0 auto;
			}

			.post-card {
				background: rgba(255, 255, 255, 0.96);
				border-radius: 16px;
				overflow: hidden;
				box-shadow: 0 10px 30px rgba(var(--gray), 0.08);
				transition: var(--transition-base);
				text-decoration: none;
				display: flex;
				flex-direction: column;
				border: 1px solid rgba(var(--gray), 0.08);
				backdrop-filter: blur(10px);
				-webkit-backdrop-filter: blur(10px);
			}

			.post-card:hover {
				transform: translateY(-2px);
				box-shadow: 0 18px 40px rgba(var(--gray), 0.16);
				border-color: rgba(var(--accent), 0.35);
			}

			.post-image-wrapper {
				position: relative;
				height: 180px;
				overflow: hidden;
			}

			.post-image {
				width: 100%;
				height: 100%;
				object-fit: cover;
				transition: transform 0.3s ease;
			}

			.post-card:hover .post-image {
				transform: scale(1.02);
			}

			.post-content {
				padding: 1.2rem 1.1rem 1.1rem;
			}

			.post-title {
				font-size: 1.2rem;
				font-weight: 600;
				color: rgb(var(--black));
				margin: 0 0 0.8rem 0;
				line-height: 1.4;
				transition: color 0.3s ease;
				display: -webkit-box;
				-webkit-line-clamp: 2;
				-webkit-box-orient: vertical;
				overflow: hidden;
			}

			.post-card:hover .post-title {
				color: var(--accent);
			}

			.post-meta {
				display: flex;
				align-items: center;
				color: rgb(var(--gray));
				font-size: 0.85rem;
				gap: 0.8rem;
			}

			.post-date {
				display: flex;
				align-items: center;
			}

			.post-date svg {
				width: 13px;
				height: 13px;
				margin-right: 0.4rem;
				fill: currentColor;
			}

			/* 分类和标签样式 */
			.post-category {
				display: inline-block;
				padding: 0.3rem 0.8rem;
				background: rgba(var(--accent), 0.1);
				color: var(--accent);
				border-radius: 20px;
				font-size: 0.75rem;
				font-weight: 600;
				margin-bottom: 0.8rem;
				text-transform: uppercase;
				letter-spacing: 0.5px;
			}

			.post-tags {
				display: flex;
				gap: 0.6rem;
				margin-bottom: 1rem;
				flex-wrap: wrap;
			}

			.post-tag {
				padding: 0.2rem 0.6rem;
				background: rgba(var(--gray), 0.1);
				color: rgb(var(--gray));
				border-radius: 20px;
				font-size: 0.7rem;
				font-weight: 500;
				transition: all 0.2s ease;
			}

			.post-tag:hover {
				background: rgba(var(--gray), 0.2);
				color: rgb(var(--black));
			}

			/* 响应式优化 */
			@media (max-width: 768px) {
				.latest-posts-section {
					padding: 2.5rem 1.25rem 2.75rem;
				}

				.section-header {
					flex-direction: column;
					gap: 1.2rem;
					text-align: center;
				}

				.section-title::after {
					left: 50%;
					transform: translateX(-50%);
				}

				.section-subtitle {
					max-width: none;
				}

				.posts-grid {
					grid-template-columns: 1fr;
				}

				.post-image-wrapper {
					height: 160px;
				}

				.post-content {
					padding: 1rem 0.8rem;
				}

				.post-title {
					font-size: 1.1rem;
				}

				.post-category {
					font-size: 0.7rem;
					padding: 0.25rem 0.7rem;
				}

				.post-tags {
					gap: 0.4rem;
				}

				.post-tag {
					font-size: 0.65rem;
					padding: 0.15rem 0.5rem;
				}
			}

			@media (max-width: 480px) {
				.section-title {
					font-size: 1.6rem;
				}

				.post-image-wrapper {
					height: 140px;
				}

				.post-content {
					padding: 0.9rem 0.8rem 1rem;
				}

				.post-category {
					font-size: 0.65rem;
					padding: 0.2rem 0.6rem;
				}

				.post-tag {
					font-size: 0.6rem;
					padding: 0.1rem 0.4rem;
				}
			}

			/* 夜间模式样式 */
			.night-mode .latest-posts-section {
				background: transparent;
			}

			.night-mode .section-title {
				color: #f5f5f5;
			}

			.night-mode .section-subtitle {
				color: #b0b0b0;
			}

			.night-mode .post-card {
				background: #1f1f23;
				border-color: rgba(255, 255, 255, 0.06);
				box-shadow: 0 16px 40px rgba(0, 0, 0, 0.7);
			}

			.night-mode .post-title {
				color: #f9fafb;
			}

			.night-mode .post-meta {
				color: #9ca3af;
			}

			.night-mode .post-category {
				background: rgba(var(--accent), 0.22);
				color: #f9fafb;
			}

			.night-mode .post-tag {
				background: rgba(255, 255, 255, 0.06);
				color: #a5b4fc;
			}

			.night-mode .post-tag:hover {
				background: rgba(255, 255, 255, 0.14);
				color: #e5e7eb;
			}

			.night-mode .view-all-button {
				background: rgba(15, 23, 42, 0.9);
				color: #e5e7eb;
				border-color: rgba(148, 163, 184, 0.6);
			}

			.night-mode .view-all-button:hover {
				background: var(--accent);
				color: #fff;
			}
		</style>
	</body>
</html>
